# public/.htaccess
RewriteEngine On
RewriteBase /

# Si no existe el fichero o directorio, redirige a index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
